{
  "name": "${project.artifactId}",
  "version": "1.0.0",
  "description": "",
  "main": "webpack.config.js",
  "dependencies": {
    "babel-standalone": "${version.babel}",
    "react": "${version.reactjs}",
    "react-dom": "${version.reactjs}",
    "webpack": "${version.webpack}"
  },
  "devDependencies": {
    "json-loader": "^0.5.4"
  }
}
